import React from 'react';
import { useSelector, useDispatch } from 'react-redux';

function Categories() {
  return (
    <div>
      <button type="button">
        Check Status
      </button>
    </div>
  );
}

export default Categories;
